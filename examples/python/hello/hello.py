class WitWorld:
    def hello(self, name: str) -> str:
        """Return a greeting for the given name."""
        return f"Hello, {name}!"

    def say(self, message: str) -> str:
        """Echo a message back."""
        return message
